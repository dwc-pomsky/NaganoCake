class CartItem < ApplicationRecord

  # has_one :cart_itemsを、customer.rbに記述?
  # belongs_to :customerをcart_item.rbに記述?

  # belongs_to :cart_itemsを、item.rbに記述?
  # has_many :itemsを、cart_item.rbに記述?

end
