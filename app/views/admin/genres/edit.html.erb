<h2 style="position:relative; top:10px; left:200px;">ジャンル一覧・追加</h2>

<div class="row">
  <%= form_with model: @genre, url: admin_genre_path(@genre.id),method: :patch, local: true do |f| %>
    <%= f.label :"ジャンル名" %>
    <%= f.text_field :name %>
    <%= f.submit "変更を保存",class: 'btn btn-success' %>
  <% end %>
</div>