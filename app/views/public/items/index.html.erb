<!--部分テンプレートの呼び出し(サイドバー)-->

<h1>商品一覧</h1>
<p>(全count件)</p>

<% @items.each do |item| %>
  <div class="col">
   <p><%= attachment_image_tag item,:image,:fill, 40, 60 %></p>
   <p class="font-weight-bold"><%= link_to item.name, '/items/:id' , class: "item_#{item.id}" %></p>
   <p class="font-weight-bold">¥<%= item.price %></p>
  </div>
<% end %>

<!--paginate(kaminari)-->